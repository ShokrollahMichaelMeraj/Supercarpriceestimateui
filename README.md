
  # Supercar Price Estimate UI

  This is a code bundle for Supercar Price Estimate UI. The original project is available at https://www.figma.com/design/z8tyWTkPcLNZlIpaBm71IQ/Supercar-Price-Estimate-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  